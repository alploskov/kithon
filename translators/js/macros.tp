min:
  alt_name: "Math.min"
  type: float

max:
  alt_name: "Math.max"
  type: float

round:
  args: [num, ndigits]
  code: "+{{num}}.toFixed({{ndigits}})"

print:
  alt_name: "console.log"
  rettype: "None"

input:
  alt_name: "prompt"
  type: "func"
  rettype: "str"

len:
  code: "{{args[0]}}.length"
  rettype: int

interval:
  alt_name: "setInterval"
  type: "Func"
  ret_type: "None"

any.//.any:
  code: "Math.floor({{left}}/{{right}})"
  type: "int"
